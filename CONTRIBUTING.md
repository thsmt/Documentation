# 執筆ルール

実際の表示とHTML記法は [writing-style-guide.html](writing-style-guide.html) で確認する

## ルール更新方針

- レビューで複数ページに共通する指摘が出た場合は、対象ページの修正と同時に本ファイルへ再発防止ルールを追記する
- 指摘内容はページ名や一時的な修正履歴ではなく、今後のページにも適用できる判断基準、HTML例、検証項目として記載する
- 表示方法やHTML記法を追加または変更した場合は、必要に応じて [writing-style-guide.html](writing-style-guide.html) の表示見本も更新する
- 共通CSSまたはJavaScriptを変更した場合は、特定ページだけでなく全HTMLへの影響を確認する
- 重複、矛盾、不要になったルールを見つけた場合は追記だけで済ませず、既存の記述を統合または削除して最新状態を保つ

## 表記

- 日本語の説明文、箇条書き、表セルの文末に句点 `。` を付けない
- 説明文と表セルの最終文は、`する`、`できる`、`ない` などの常体で終える
- 番号付き手順と `procedure-step` は、`確認`、`選択`、`作成` などの操作名で終える
- AWSサービス名、OS名、製品名は公式名称を使用する
- 同じ操作や構成要素は全ページで同じ見出し名を使用する
- プレースホルダーは `<UPPER_SNAKE_CASE>` 形式に統一する

## 説明文

- 対象、実施する理由、制約、実施方法が一読で分かる文章にする
- コンソールで実施できずAWS CLIのみで変更できる手順は、その仕様を冒頭で明記する
- 1つの段落には原則として1つの論点を記載する
- 概要、条件、実行内容、出典を1つの段落へ詰め込まない
- コマンドの説明は「何を確認または変更するか」が分かる通常の段落とし、説明文だけを小見出しや番号付き見出しへ変更しない
- コマンド出力や設定値を示す `{}`、`true`、`false`などは通常の等幅書体で記載し、レビュー用マーカーでは強調しない
- 表セルで説明と短い例を併記する場合は、段落を増やさず `<br>` で改行する
- AWSサービスの制約は「できる／できない」だけで表現せず、AWS公式仕様に合わせて「サポートされる／サポートされない」など適用範囲が分かる表現にする
- 「場合があります」のような曖昧な表現は避け、条件を確認できる場合は具体的に記載する

良い例：

> EC2インスタンス起動後に追加したEBS終了設定（DeleteOnTermination）を変更する場合は、AWS CLIのみで変更可能

避ける例：

> EC2起動後にEBSボリュームの設定を変更する場合は、AWS CLIでコマンドを実行する。追加EBSボリュームは終了時削除が無効になっている場合があるため確認する

## 構成

- ページ冒頭には空でないリード文を1つだけ置き、目的と対象範囲を簡潔に記載する
- コマンドの説明をコードブロックの直前に置く
- 同じ論点の説明を複数行で示す場合は、複数の段落へ分けず、1つの`<p>`内を`<br>`で改行する
- 出典は本文と同じ論点であっても独立した段落にし、本文との境界を明確にする
- 画像が変更前後の状態や操作結果を示す場合は、説明を画像の上へ置き、`<figure>`の最初の子要素として`<figcaption>`を記載する
- コマンドは言語名とCopyボタンを備えた共通コードブロックで記載する
- 一連の操作は番号付き見出しと小番号で整理する
- 順番が重要な概要手順は、表ではなく `<ol class="step-list">` の手順カードで示す
- OSごとの差分はタブまたは比較表で示す
- 情報パネルは `ℹ️ 情報`、`⚠️ 注意`、`✅ 成功`、`❌ エラー`を使用する
- 情報パネルの見出しには、パネルの種類に対応する記号を付ける
- ページ内見出しは各ページの `<aside class="toc">` に記載し、右側へは表示しない
- 共通JavaScriptがページ内見出しを左サイドバーの現在ページ直下へ展開するため、サイドバーへ子項目を重複して記載しない

### ページ要約

- ページタイトル直下の既存リード文を、`⚡ このページの要約`として統一表示する
- HTMLには空でない `<p class="lead">` を各ページに1つだけ記載する
- 要約は対象、目的、対象範囲が分かる1文または2文にまとめる
- 要約はページの用途を利用者が判断できる内容とし、機能の羅列や本文と重複する長い背景説明を避ける
- 2文で構成する場合は、1行目に対象と用途、2行目に手順の構成または利用場面を記載する
- 同じ要約内で文を分ける場合は、段落を追加せず `<br>` で改行する
- 要約と同じ内容を示す「目的」「使い分け」などの枠を重複して設置せず、必要な説明は要約へ統合する
- `⚡ このページの要約`の見出しは20px、左側のアクセント線は灰色の5pxとし、ページ個別のCSSで上書きしない

要約のHTML例：

```html
<p class="lead">AWSマネジメントコンソールでは変更できない設定について、AWS CLIを使用した対応方法をまとめる<br>GUIに操作項目がない場合の確認手順と実行コマンドを整理する</p>
```

## コマンドブロック

- コマンドは原則として1つのコマンドにつき1つのコードブロックへ分ける
- 変数への代入、ユーザーやリソースの作成、設定変更など、単独で意味を持つ操作は個別のコードブロックにする
- 実行頻度が低い構築コマンドや、結果を確認してから次へ進むコマンドは個別のコードブロックにする
- サービス状態の確認、起動設定、再確認など、1つの連続した確認手順は同じコードブロックにまとめてよい
- ファイル作成と直後の内容確認など、同一目的で連続実行する短いコマンド群は同じコードブロックにまとめてよい
- 複数コマンドをまとめる場合は、上から順番に安全に実行でき、途中結果を利用者が確認できる構成にする

個別のコードブロックにする例：

```powershell
$Password = Read-Host "Password for <CUSTOMER_USER>" -AsSecureString
```

```powershell
New-LocalUser -Name "<CUSTOMER_USER>" -Password $Password -Description "<CUSTOMER_USER>"
```

連続した操作としてまとめてよい例：

```bash
systemctl is-enabled postfix.service
systemctl enable postfix.service
systemctl is-enabled postfix.service
```

### 行番号、背景色、コピー

- 共通コードブロック内のコードには、すべてJavaScriptとCSSで行番号を表示する
- HTMLのコード本文へ行番号を直接記載しない
- コード本文はライトモードとダークモードの両方で黒背景にする
- Copyボタンは表示用の行番号を除外し、HTMLに記載した元のコード文字列だけをコピーする
- 空行を含む複数行コードでは、表示行数とコピー後の行数が一致することを確認する
- ページ固有のクラスへ依存せず、`main.content .code pre code`へ共通処理を適用する

## 文字の強調

- 利用者が置換する値は `<UPPER_SNAKE_CASE>` 形式にし、オレンジ色で表示する
- `<INSTANCE_ID>`や`<REGION>`はプレースホルダーと呼び、入力項目をまとめる表では入力パラメータとして扱う
- コマンド内のプレースホルダーはオレンジ色で表示し、入力パラメータの説明表では表の文脈が明確なため通常色で表示してよい
- 説明文中で利用者がコマンドや設定ファイルへ入力するパラメータ値は、オレンジ色の等幅書体で表示する
- 画面操作で表示される項目名、選択値、ボタン名は、薄いグレーの背景と枠線を持つ等幅書体で表示する
- 画面操作用の囲い文字は、共通CSSで前後に半角スペース相当の間隔を1つだけ設け、本文へ密着させない
- コンソールの階層を順に開く操作は、`項目 ＞ 項目 ＞ 項目`の形式で操作経路を記載する
- 固定のコマンド名、APIアクション、プロパティ名は通常色の等幅書体で表示し、プレースホルダーと区別する
- 色分けは値の文字列ではなく、利用者が環境ごとに決定する値か、手順や製品仕様で確定する値かで判断する
- コピーして実行するコード内の `true`、`false`、`NONE`など、利用者が置換しない固定値は通常色で表示する
- 真偽値や列挙値を利用者が要件に応じて決定する場合は、`<ENABLED>`や`<ACTION_AFTER_COMPLETION>`などのプレースホルダーとしてオレンジ色で表示し、許容値を説明する
- 利用者が作成したIAMロール、IAMポリシー、リソースの確定済み名称は通常色で表示する
- AWS管理ポリシー、サービスリンクロール、APIアクションなど、AWSが名称を定義して利用者が変更しない識別子も通常色で表示する
- 所有元を示すバッジは使用せず、必要な場合は周囲の説明文でAWS管理または利用者作成であることを示す
- 太字は結論や重要条件、`kbd` はキーボード操作、バッジは必須・任意などの短い状態表示に使用する
- `必須`バッジは赤地に白文字、`任意`バッジは青地に白文字で表示する
- 色だけに意味を依存せず、周囲の文章でも入力値、固定値、必須条件を説明する
- `<mark>`は公開前の一時的なレビュー対象だけに使用し、通常の本文、コマンド出力、設定値の強調には使用しない

色分けの判断順序：

1. 利用者が置換するプレースホルダーはオレンジ色にする
2. 画面上で選択する値は薄いグレーの画面操作用書式、コマンドや設定ファイルで置き換える値はオレンジ色のプレースホルダーとして記載する
3. 利用者作成またはAWS管理を問わず、確定済みのリソース名やポリシー名は通常色にする
4. 手順で確定しており、コピー後に変更しない値は通常色にする
5. AWSが正式名称を定義している識別子は通常色にする

利用者が置き換える値：

```html
<code class="user-value">&lt;ROLE_NAME&gt;</code>
```

固定のコマンド名やAPIアクション：

```html
<code class="inline-code">scheduler:GetWidget</code>
```

画面上の項目名、選択値、ボタン名：

```html
<code class="ui-value">NONE</code>
```

コンソールの操作経路：

```html
CloudWatchコンソールで<code class="ui-value">メトリクス</code> ＞ <code class="ui-value">すべてのメトリクス</code> ＞ <code class="ui-value">Scheduler</code>を開く
```

文章中の例：

```html
<code class="ui-value">Schedule</code>を有効化し、完了後のアクションは<code class="ui-value">NONE</code>、再試行は<code class="ui-value">OFF</code>、DLQは<code class="ui-value">なし</code>、暗号化は<code class="ui-value">デフォルト</code>とする。<code class="ui-value">既存のロールを使用</code>を選択し、<code class="inline-code">EventBridgeSchedulerEC2Stop</code>を指定する
```

## 検証

- 文体を一括整形する場合は `powershell -File scripts/normalize-writing-style.ps1` を実行する
- 文末表現と句点は `powershell -File scripts/check-writing-style.ps1` で確認する
- ページ内リンクは `powershell -File scripts/check-page-links.ps1` で確認する
- ページ構造と共通要素は `powershell -File scripts/check-page-consistency.ps1` で確認する
- 全HTMLで要約が1件だけ表示され、見出し、文字サイズ、アクセント線、本文が正しいことをブラウザーで確認する
- 全コードブロックで行番号が表示され、ライトモードでも本文が黒背景になることをブラウザーで確認する
- 単一行と複数行のコードをCopyボタンでコピーし、行番号を含まない元のコード文字列と一致することを確認する

## 情報源

- 期限、制約、仕様は公式ドキュメントを優先する
- 外部記事を参照する場合も公式ドキュメントで裏付ける
- 参照リンクは該当する説明や手順の直下に配置する
- 出典は説明文へ埋め込まず、独立した段落にする
- 出典ラベルは `出典：` に統一する
- User Guideなどの仕様説明ページを優先し、同じ内容を示すCLIコマンドリファレンスは重複掲載しない
- CLIコマンドリファレンスしか根拠がない場合は、出典として掲載してよい

## 校正チェックリスト

- 冒頭だけで対象、理由、制約、実施方法が分かる
- ページ冒頭に空でない要約が1件だけあり、重複する目的枠がない
- 要約内の文の区切りは別段落ではなく改行になっている
- 要約が利用場面と本文の構成を簡潔に示し、本文と重複していない
- AWS CLIのみで実施できる手順は、その仕様が明記されている
- 説明文、条件、出典が読みやすい段落へ分かれている
- コマンドブロックの分割または連続記載に理由がある
- 全コードブロックに表示用の行番号があり、コピー内容には行番号が含まれていない
- ライトモードとダークモードの両方でコード本文が黒背景になっている
- プレースホルダーと利用者が指定する値がオレンジ色で判別できる
- 固定のコマンド名やAPIアクションがプレースホルダーと区別されている
- 変更前の確認、実行、変更後の確認、必要な切り戻しが揃っている
- 画像の状態説明が画像より前に表示されている
- 表セル内の短い例が改段落ではなく改行で表示されている
- 公式ドキュメントで仕様とコマンドを確認している
- 自動チェックとブラウザー表示確認が完了している
